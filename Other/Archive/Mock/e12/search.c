#include <stdio.h>
#include <math.h>

#define DIM 9

int main()
{
    char arr[DIM] = {'a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i'};
    char key;
    int half = DIM / 2;

    printf("inserire la lettera da cercare: \n");
    scanf("%c", &key);
    fflush(stdin);

    printf("%c", key);
}

int lenght(char *arrPointer)
{
    for
}