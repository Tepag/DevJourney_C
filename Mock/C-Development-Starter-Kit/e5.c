// cambia il valore di una variabile dichiarata nel main, usando le funzioni e il puntatore

#include <stdio.h>

void changeValue(int *);

int main()
{
    int value = 0;

    // chiamo la funzione
}

changeValue(int *pointer)
{
    // scrivi qua il tuo codice
}